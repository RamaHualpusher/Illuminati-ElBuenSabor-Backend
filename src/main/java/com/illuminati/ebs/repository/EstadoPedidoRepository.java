package com.illuminati.ebs.repository;

import com.illuminati.ebs.entity.EstadoPedido;
import org.springframework.stereotype.Repository;

@Repository
public interface EstadoPedidoRepository extends GenericRepository<EstadoPedido, Long> {
}
